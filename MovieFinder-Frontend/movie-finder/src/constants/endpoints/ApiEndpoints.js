const API_BASE_URL = "https://localhost:7141/api";

const API_ROUTES = {
  GET_MOVIE: `${API_BASE_URL}/Movie/GetMovie`,
  GET_ALL_MOVIE: `${API_BASE_URL}/Movie/GetAllMovie`,
  // Diğer API rotalarını buraya ekleyebilirsiniz
};

export default API_ROUTES;
